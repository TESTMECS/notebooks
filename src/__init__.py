"""
These are notebooks are sufficient on their own.
File is runnable with `uv run <file>.py`
The `public/` folder contains two directories:
- `pages/` - contains index.html pages corresponding to each notebook script.
- `public/` - contains the outputs of the notebooks.
- `data/` - will be created to store the data downloaded by any of the notebooks. Working on sqlite3 integration.
"""

